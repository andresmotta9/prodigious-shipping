import { EnumoptionPipe } from './enumoption.pipe';

describe('EnumoptionPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumoptionPipe();
    expect(pipe).toBeTruthy();
  });
});
